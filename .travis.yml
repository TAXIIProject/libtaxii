language: python
env:
   - TOXENV=py26
   - TOXENV=py27
   - TOXENV=rhel6

install:
   - pip install tox
   - pip install pep8
script:
   - tox
   - pep8 --exclude=.tox,build --ignore=E501

notifications:
    email:
        - taxii-commits-list@lists.mitre.org
